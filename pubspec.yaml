name: easypark_flutter
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.6.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.11.0
  google_maps_webservice: ^0.0.20-nullsafety.5 # Add this line
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.3 
  provider: ^6.1.2
  google_maps_flutter: ^2.10.0
  google_maps_flutter_web: ^0.5.10
  geolocator: ^13.0.2
  js: ^0.6.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.jpg  # ✅ Ensure the file exists exactly as written

